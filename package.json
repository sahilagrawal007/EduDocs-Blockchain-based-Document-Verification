{
  "name": "student-certs-prototype",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "node": "npx hardhat node",
    "deploy-local": "npx hardhat run --network localhost scripts/deploy.js",
    "start-all": "node dev-start.js"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^5.7.2",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "form-data": "^4.0.5",
    "node-fetch": "^3.3.2"
  }
}
